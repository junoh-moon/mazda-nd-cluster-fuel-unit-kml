---
name: Report your car's 720-01-01
about: Add a data point to the region × unit-field table (works or not, both are useful)
title: "[data] <model> <year> <market>: <unit shown>"
labels: data
---

**Car**
- Model / generation / year:
- Market the car was built for (and where it lives now):
- IC part number and strategy (FORScan → Vehicle info → IC):
- CMU firmware version (Mazda Connect → Settings → System → About):
- CMU language in use:

**Factory `720-01-01`** (as read by FORScan, e.g. `27E1 E226 80B9`):

**Unit shown on the cluster with the factory value** (km/L, L/100km, mpg):

**What you tried** (one row per write, keep the checksum):

| 720-01-01 written | Read back OK? | Sleep cycle done (adapter unplugged, ≥5 min)? | Cluster unit | CMU screen unit | Other changes noticed |
|---|---|---|---|---|---|
| | | | | | |

**DTCs before / after** (module and code, or "none"):

**Anything else** (temperature unit, language, warning text, TPMS/seat-belt behaviour):
